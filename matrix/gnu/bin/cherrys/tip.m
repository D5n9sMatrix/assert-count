#!/usr/bin/wolframscript -cloud -code

(* This is node kennel this to support jacket 802 Mg 06 position to analysis 
of position of equation linear rank equation to support linear to make formation 
mash my blimey position contemplate packages linear to object require auto classic 
about position numeric of values in agreement's transport any MG 306 TIP to solver 
the mathematics linear of object peps position values to metaphor's 
playing of stability to position functional.*)

j k + Log[802 ((m g)/6)] == Sin[(m g)/6]
j k + Log[(401 g m)/3] == Sin[(g m)/6]
FunctionExpand[j k + Log[(401 g m)/3] == Sin[(g m)/6]]
j k == -Log[(401 g m)/3] + Sin[(g m)/6]
TrigToExp[j k + Log[(401 g m)/3] == Sin[(g m)/6]]
Simplify[PowerExpand[j k + Log[(401 g m)/3] == Sin[(g m)/6]]]
PowerExpand[j k + Log[(401 g m)/3] == Sin[(g m)/6], Assumptions -> {}]