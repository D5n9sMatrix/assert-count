#!/usr/bin/wolframscript -cloud -code

(* 
# Its lever is on its immeasurable magnitude on the equilibrium of
# its weight in the same way its magnitude and equilibrium of its
# proportional distance its health is immeasurable and its equilibrium
# and on the unit of its hard disk strengthening its arms in a mechanism
# of functional utility to its geometrical proportions organism to
# strengthen its basic in logical unity. *)

Range[127] -> With[{StirlingS1 = {I10, 5}}, Table[I10^2, StirlingS1]]
DiagonalMatrix[{A1, B1, C1}] (* MatrixForm *)
DiagonalMatrix[{A1, B1}, 1]  (* MatrixForm *)
DiagonalMatrix[{A1, B1}, -1] (* MatrixForm *)
MatrixForm[M2 = Array[Subscript[A1, ##] &, {3, 3}]] -> A1^Range[99]
Map[MatrixForm, {MD, MO}] -> A1^Range[99]
M2 = N[HilbertMatrix[3]] -> A1^Range[99] + An^Range[99]
{M2, MD} = Eigensystem[MO] -> A1^Range[99] + An^Range[99]
Transpose[MD] . DiagonalMatrix[M2] . MathieuGroupM12
jordanMatrix[\[Lambda]_, n_] := 
DiagonalMatrix[Table[\[Lambda], {n}]] + 
DiagonalMatrix[Table[1, {n - 1}], 1]
jordanMatrix[\[Lambda], 3] // MatrixForm
n = 5;
MatrixForm[
DiagonalMatrix[Array[Subscript[A1, #] &, n - 1], -1] + 
DiagonalMatrix[Array[Subscript[B1, #] &, n]] + 
DiagonalMatrix[Array[Subscript[C1, #] &, n - 1], 1]]
m = ( {
    {A1, 0},
    {0, B1},
    {0, 0}
   } );